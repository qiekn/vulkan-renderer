# Drop audio clips here (WAV, OGG, FLAC, MP3). Referenced by name from
# `Application::Run` (ep6 loads `ambient.ogg` + `ping.wav` if present).
# miniaudio decodes at load time — no preprocessing needed.
